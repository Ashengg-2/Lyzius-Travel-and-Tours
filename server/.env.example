# Match repo root docker-compose.yml (`docker compose up -d` — maps host :5433 → Postgres :5432):
DATABASE_URL="postgresql://lyzius:lyzius@localhost:5433/lyzius"

# Or managed Postgres (Render / Neon external URL):
#DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"

JWT_SECRET="replace-with-long-random-string-at-least-32-chars-for-dev"
FRONTEND_ORIGIN="http://localhost:5173"
PORT=3333
