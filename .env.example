# Flask production (copy to Render Environment or local .env)
# Do NOT commit real secrets to Git.

# Required in production
SECRET_KEY=replace-with-long-random-string

# Optional — only if you attach a Render persistent disk at /data
# DATABASE_URL=sqlite:////data/lyzius.db

# Render injects PORT automatically; no need to set it manually.
